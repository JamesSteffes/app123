import React from 'react'

const Header = (props) => {
    return (
        <div>
            Header
            from new components
            <br />
            {props.date}

        </div>
    )
}

export default Header